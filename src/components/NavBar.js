import React from "react";
import {Container, FlexRow} from "../utils/containers";

const NavBar = () => {
    return (
        <Container as='header'>
            <FlexRow as='nav'>

            </FlexRow>
        </Container>
    );
}

export default NavBar;